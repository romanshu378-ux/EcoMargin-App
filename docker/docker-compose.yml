version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: ecomargin-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: ecomargin
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secretpassword
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: ecomargin-redis
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data

volumes:
  pgdata:
  redisdata:
